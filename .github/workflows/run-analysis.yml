name: Run final gate checks

on:
  workflow_dispatch:
  push:
    paths:
      - 'analysis/gate_check_min.py'
      - '.github/workflows/run-analysis.yml'

permissions:
  contents: write

jobs:
  run-final-gate-checks:
    runs-on: ubuntu-latest
    timeout-minutes: 45

    steps:
      - name: Checkout
        uses: actions/checkout@v4

      - name: Set up Python
        uses: actions/setup-python@v5
        with:
          python-version: '3.11'

      - name: Install dependencies
        run: |
          python -m pip install --upgrade pip
          pip install torch sentence-transformers scikit-learn pandas numpy matplotlib tabulate

      - name: Run final gate checks
        run: python analysis/gate_check_min.py

      - name: Commit outputs
        run: |
          git config user.name "github-actions[bot]"
          git config user.email "41898282+github-actions[bot]@users.noreply.github.com"
          git add analysis_outputs
          if git diff --cached --quiet; then
            echo "No output changes"
          else
            git commit -m "Add final gate check outputs"
            git push
          fi
